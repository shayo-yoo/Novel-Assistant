# VSCode 小说写作辅助扩展 功能规划书（AI 可读规格说明）

## 0. 文档说明

本文档是“VSCode 小说写作辅助扩展”的唯一功能事实来源。  
当本文档与历史对话、零散想法冲突时，以本文档为准。  
本文档面向 AI 与开发者，用于理解扩展应实现什么、不实现什么、如何交互、如何计算、如何存储。

需求等级说明：

- **MUST**：必须实现，否则视为未完成核心目标。
- **SHOULD**：建议实现，能显著提升体验。
- **MAY**：可选实现，用于后续增强。
- **默认值**：若用户未配置，则使用该值。
- **可配置**：用户可在工作区或全局设置中修改。

---

## 1. 产品定义

### 1.1 产品名称

暂定名：**小说助手 / Novel Assistant**。  
VSCode 扩展 ID 可在开发时确定，例如 `local.novel-assistant`。

### 1.2 目标用户

- 中文网络小说作者。
- 使用 VSCode 编写 `.txt` 小说草稿的用户。
- 需要字数统计、写作计时、输入速度、格式化、高亮、剧情管理的创作者。
- 希望不同小说工作区拥有独立配置的用户。

### 1.3 核心定位

- **轻量**：不强制改变用户写作习惯。
- **直观**：所有常用设置通过中文命令、右键菜单、侧边栏、QuickPick 完成，不要求用户手改 JSON 或输入复杂英文命令。
- **工作区隔离**：不同小说工作区配置互不影响。
- **本地优先**：所有文本、统计、高亮、敏感词数据默认仅保存在本地，不上传云端。
- **低干扰**：状态栏与侧边栏提供信息，但不遮挡正文。

### 1.4 支持文件格式

- 主要支持：`.txt`。
- 可选支持：`.md`、`.text`、`.novel`。
- 默认只对 `.txt` 启用核心功能。
- 文件扩展名列表可配置。

---

## 2. 术语与全局规则

### 2.1 工作区

VSCode 当前打开的 workspace 或 folder。  
多根工作区中，每个根文件夹视为一个独立“小说工作区”。  
当前活动编辑器所属的根文件夹，称为“当前工作区”。

### 2.2 当前小说

当前工作区内，符合配置的文件扩展名，且未被排除规则排除的文本文件集合。  
默认：所有 `.txt` 文件。

### 2.3 章节

默认一个文本文件视为一个章节。  
章节顺序默认按文件名自然排序。  
用户可在章节面板中手动调整顺序或分组。

### 2.4 窗口活动

满足以下条件时，视为“窗口活动”：

- VSCode 窗口是操作系统前台窗口。
- VSCode API 中 `window.state.focused === true`。

窗口失焦、最小化、被其他应用遮挡并失去焦点时，均视为“非窗口活动”。

### 2.5 创作时间

创作时间 = 当前小说处于窗口活动状态下的累计时间。  
规则：

- 窗口活动时，每秒累计。
- 窗口失焦时，立即暂停累计。
- 空闲但不失焦时，默认**不暂停**创作时间，因为用户可能正在思考。
- 可配置“空闲暂停创作时间”，默认关闭。
- 创作时间按工作区、按日期、按文档分别累计。

### 2.6 有效输入时间

在创作时间内，实际发生“非粘贴新增字符”的秒数。  
用于计算输入速度。  
空闲、只删除、只移动光标、只粘贴大段文字的时间，不计入有效输入时间。

### 2.7 新增字符

文本变更事件中插入的文本，按 Unicode 码点计数。  
删除、替换中的删除部分不计入新增。  
撤销、重做产生的文本增加默认不计入写作速度，但可能计入字数变化。  
实际实现无法完全区分撤销与输入，采用近似规则。

### 2.8 粘贴判定

单次文本变更插入字符数大于 `pasteThresholdChars`，且距离上一次输入小于 `pasteIntervalMs`，视为粘贴。  
默认：

- `pasteThresholdChars = 30`
- `pasteIntervalMs = 500`

粘贴内容：

- 计入字数统计。
- 不计入输入速度。
- 不计入有效输入时间。

### 2.9 半角空格

半角空格 = Unicode `U+0020`。  
“四个半角空格” = `"    "`，即四个连续 `U+0020`。  
不是全角空格 `U+3000`，不是 Tab `U+0009`。

### 2.10 高亮

高亮仅通过 VSCode 装饰器改变显示效果，不修改文本内容。  
高亮可分组、可配色、可按工作区独立配置。

### 2.11 番茄钟

一种时间管理循环：专注工作一段时间，然后短休息，若干循环后长休息。  
默认：工作 25 分钟，短休 5 分钟，每 4 个番茄后长休 15 分钟。

---

## 3. 功能模块详细规格

## 3.1 字数统计

### 3.1.1 目标

提供章节字数统计、全书字数统计、选中文本字数统计、工作区字数统计。  
支持多种计数模式，适配不同网文平台与创作环境。  
字数统计与创作时间、输入速度、每日目标关联。

### 3.1.2 统计范围

MUST 支持：

- 当前文档字数。
- 当前选中文本字数。
- 当前工作区全部章节字数。
- 当前章节在侧边栏中的字数。
- 今日新增字数。
- 全书总字数。

SHOULD 支持：

- 按文件夹分组统计。
- 按章节状态统计，如草稿、修改中、完成。

### 3.1.3 触发方式

- 状态栏常驻显示当前文档字数。
- 命令面板：`小说助手：切换字数统计模式`。
- 命令面板：`小说助手：查看字数统计详情`。
- 侧边栏“章节面板”显示每章字数。
- 选中文本后，状态栏或侧边栏显示选中字数。
- 保存文件时自动刷新。

### 3.1.4 实时更新

- 文档变化后防抖 `300ms` 更新。
- 大文件超过 `5MB` 时，可降低更新频率至 `1000ms`。
- 统计计算应在后台线程或异步任务中执行，不阻塞输入。

### 3.1.5 内置计数模式

MUST 提供以下模式，用户可在状态栏或 QuickPick 中切换：

#### 模式 1：全字符模式 `all`

- 统计所有 Unicode 码点。
- 包括空格、Tab、换行、标点、汉字、英文、数字、emoji。
- 每个码点计 1。
- 换行符：`\n` 和 `\r\n` 按实际码点计数，`\r\n` 计 2。

#### 模式 2：仅中文模式 `cjk`

- 仅统计 CJK 统一汉字及其扩展区字符。
- 包括：`\u4E00-\u9FFF`、`\u3400-\u4DBF`、扩展 B-F 区等。
- 不包括中文标点、英文、数字、空格、换行。
- 每个汉字计 1。

#### 模式 3：中文加标点模式 `cjk_punct`

- 统计 CJK 汉字。
- 统计中文常用标点：，。！？；：“”‘’《》【】（）——……·等。
- 不包括空格、换行、英文、数字。
- 每个汉字或标点计 1。

#### 模式 4：非空白字符模式 `non_whitespace`

- 排除所有 Unicode 空白字符。
- 空白包括：半角空格、全角空格、Tab、换行、回车、换页等。
- 其余每个 Unicode 码点计 1。
- 标点、汉字、英文、数字均计 1。
- 该模式接近“排除空格和换行后的总字符数”。

#### 模式 5：起点模式 `qidian`

- 默认近似起点作者后台常见口径。
- 排除所有 Unicode 空白字符。
- 其余每个 Unicode 码点计 1。
- 即与 `non_whitespace` 相同。
- 平台规则可能变化，用户可自定义覆盖。

#### 模式 6：番茄模式 `fanqie`

- 排除所有 Unicode 空白字符。
- 连续 ASCII 字母序列计 1 个“英文词”。
- 连续 ASCII 数字序列计 1 个“数字词”。
- 其他每个 Unicode 码点计 1。
- 即：汉字、标点按字符计；英文按单词计；数字按数字串计。

#### 模式 7：中英混合模式 `mixed`

- CJK 汉字：每个计 1。
- 中文标点：每个计 1。
- 英文单词：连续 ASCII 字母序列，每个词计 1。
- 数字串：连续 ASCII 数字序列，每个串计 1。
- 其他符号：每个码点计 1。
- 空格、换行、Tab：不计。

#### 模式 8：自定义正则模式 `custom_regex`

- 用户输入一个正则表达式。
- 统计所有匹配项覆盖的字符总数。
- 若正则使用捕获组，默认统计整个匹配项。
- 用户可选择“统计匹配次数”或“统计匹配字符数”，默认“匹配字符数”。

#### 模式 9：自定义权重模式 `custom_weight`

MAY 实现。  
用户通过图形界面或 JSON 定义规则，例如：

```json
{
  "id": "my_platform",
  "name": "我的平台",
  "rules": [
    { "type": "cjk", "weight": 1 },
    { "type": "punctuation", "weight": 1 },
    { "type": "whitespace", "weight": 0 },
    { "type": "ascii_word", "weight": 1 },
    { "type": "digit_sequence", "weight": 1 }
  ]
}
```

### 3.1.6 计数排除规则

用户可配置排除：

- 标题行，例如匹配 `^第[一二三四五六七八九十百千万0-9]+章` 的行。
- 作者备注，例如 `【作者的话】` 之后内容。
- 分隔线，例如 `---`、`***`。
- 特定文件夹，例如 `.vscode`、`node_modules`。
- 特定文件 glob。

默认：

- 排除 `.vscode`、`node_modules`、`.git`。
- 不排除标题行。
- 不排除作者备注。

### 3.1.7 状态栏显示

状态栏显示格式示例：

```text
📝 12,345 字 [起点] | 今日 +1,234 | 🎯 62%
```

点击状态栏：

- 左键：打开字数统计详情面板。
- 右键：QuickPick 切换计数模式。
- 悬停：显示各模式字数、今日新增、总字数。

### 3.1.8 验收标准

- 给定文本，各模式结果符合上述算法。
- 切换模式后，状态栏与侧边栏字数立即更新。
- 重启 VSCode 后，工作区字数模式保持。
- 不同工作区可设置不同默认模式。

---

## 3.2 创作时间与输入速度

### 3.2.1 目标

累计当前小说的创作时间。  
只有窗口活动时才计时。  
提供相对准确的输入速度计算。  
统计与字数、番茄钟、每日目标联动。

### 3.2.2 计时规则

MUST：

- 监听 `window.onDidChangeWindowState`。
- 当 `window.state.focused === true` 且当前活动编辑器属于当前工作区时，开始或继续计时。
- 当 `window.state.focused === false` 时，立即暂停计时。
- 计时精度：秒。
- 数据按工作区、日期、文档累计。
- 跨天时，按本地时区切分。

SHOULD：

- 支持手动暂停/继续。
- 支持空闲暂停开关，默认关闭。
- 空闲阈值默认 `120` 秒。
- 多窗口打开同一工作区时，只有获得焦点的窗口计时，其他窗口暂停。可通过全局状态或文件锁实现。

### 3.2.3 创作时间分类

MUST 记录：

- 今日创作时间。
- 本周创作时间。
- 本月创作时间。
- 当前文档创作时间。
- 当前工作区总创作时间。
- 当前会话创作时间。

### 3.2.4 输入速度计算

输入速度定义：

```text
速度 = 最近 5 分钟内非粘贴新增码点数 / 最近 5 分钟内有效输入秒数 * 60
```

单位：字/分钟。  
中文优先，英文单词可另显示“词/分钟”。

同时显示：

- 瞬时速度：最近 60 秒。
- 短期速度：最近 5 分钟，默认显示。
- 会话平均速度：本次会话总非粘贴新增码点数 / 总有效输入秒数。
- 今日平均速度。

规则：

- 只统计非粘贴新增字符。
- 删除不计入。
- 光标移动不计入。
- 窗口失焦期间不计入。
- 空闲期间不计入有效输入秒数。
- 输入法组合期间不计数，直到文本提交到文档。

### 3.2.5 状态栏显示

示例：

```text
⏱ 今日 1h23m | ⚡ 28 字/分
```

点击：

- 左键：打开写作统计面板。
- 右键：手动暂停/继续计时。

### 3.2.6 持久化

- 按工作区保存到统计文件。
- 按日期存储每日汇总。
- 按文档存储文档累计。
- 支持导出 CSV/JSON。

### 3.2.7 验收标准

- 窗口失焦 5 秒后，创作时间停止增加。
- 窗口重新聚焦后，继续累计。
- 粘贴 500 字不增加输入速度。
- 手动输入 100 字，速度计算在合理范围内。
- 重启后今日创作时间保留。

---

## 3.3 文本格式化

### 3.3.1 目标

将小说正文的每一段开头自动设置为四个半角空格。  
主要面向 `.txt` 文档。  
不破坏原有内容，不修改行内空格。

### 3.3.2 触发方式

MUST：

- 命令面板：`小说助手：格式化当前文档`。
- 命令面板：`小说助手：格式化选中段落`。
- 右键菜单：`小说助手：格式化选中段落`。

SHOULD：

- 保存时自动格式化，默认关闭，可配置。
- 快捷键可配置。

### 3.3.3 格式化规则

MUST：

- 段落定义：以换行符分隔的非空行。
- 空行保持不变。
- 每段开头先移除所有前导半角空格、全角空格、Tab。
- 然后添加四个半角空格 `"    "`。
- 行内空格、行尾空格不修改。
- 保留原换行符类型：LF 保持 LF，CRLF 保持 CRLF。
- 支持一次撤销，通过 `WorkspaceEdit` 批量应用。

SHOULD：

- 跳过标题行，默认开启。
- 标题行规则可配置，默认匹配：
  - `^第[一二三四五六七八九十百千万零两0-9]+[章回节卷部篇]`
  - `^Chapter\s+\d+`
  - `^序章`、`^楔子`、`^尾声`、`^后记`
- 跳过分隔线：`^---$`、`^\*\*\*$`、`^___$`。
- 跳过已正确缩进的行。
- 支持多文件批量格式化当前工作区。

### 3.3.4 边界情况

- 文件为空：不操作。
- 文件只有空行：不操作。
- 段落前有多个全角空格：全部移除后加四个半角空格。
- 段落前有 Tab：全部移除后加四个半角空格。
- 段落前有混合空白：全部移除后加四个半角空格。
- 行首有 `>` 引用符号：默认不缩进，可配置。
- 行首有列表符号 `-`、`*`：默认不缩进，可配置。

### 3.3.5 验收标准

- 执行格式化后，每段开头恰好四个半角空格。
- 空行不变。
- 标题行按配置不缩进。
- 撤销可恢复原状。

---

## 3.4 高亮系统

### 3.4.1 目标

为特定字段设置文本高亮，例如主角、配角、关键道具、地点、组织、自定义词。  
设置必须直观、快速，不要求用户翻找配置文件或输入复杂英文命令。  
不同工作区可独立配置。

### 3.4.2 高亮分组

每个高亮组包含：

- `id`：唯一标识。
- `name`：中文显示名，如“主角”“配角”“关键道具”。
- `color`：背景色或边框色，十六进制或预设名。
- `enabled`：是否启用。
- `priority`：优先级，数字越小越优先，默认按组顺序。
- `words`：匹配词列表。
- `matchCase`：是否区分大小写，默认 false。
- `wholeWord`：是否全词匹配，默认 false。
- `regex`：是否按正则匹配，默认 false。
- `style`：高亮样式，背景、边框、下划线、加粗等。

默认内置分组：

- 主角：金色 `#FFD700`
- 配角：浅蓝 `#87CEEB`
- 关键道具：浅红 `#FFB6C1`
- 地点：浅绿 `#90EE90`
- 自定义：灰色 `#D3D3D3`

### 3.4.3 添加高亮词

MUST 支持以下方式：

1. 选中文本 → 右键 → `小说助手：添加到高亮组` → QuickPick 选择组 → 立即高亮。
2. 命令面板 → `小说助手：添加选中文本到高亮组`。
3. 侧边栏高亮面板 → 选择组 → 点击“添加词” → 输入词。
4. 命令面板 → `小说助手：管理高亮组` → QuickPick 选择组 → 选择“添加词”。

### 3.4.4 管理高亮组

命令：`小说助手：管理高亮组`  
QuickPick 列表显示：

- 组名、颜色块、启用状态、词数量。
- 选择组后：
  - 添加词
  - 删除词
  - 重命名组
  - 修改颜色
  - 启用/禁用
  - 设置匹配选项
  - 删除组

颜色选择：

- QuickPick 显示预设颜色，带色块。
- 支持输入自定义十六进制颜色。
- 支持“随机颜色”。

### 3.4.5 高亮渲染

- 使用 `TextEditorDecorationType`。
- 匹配到词后，应用对应装饰。
- 异步匹配，防抖 `200ms`。
- 最大匹配数默认 `5000`，可配置。
- 超出限制时，只高亮可见区域或提示用户。
- 同一文本属于多个组时，按优先级应用，高优先级覆盖低优先级。
- 高亮不修改文本。
- 切换编辑器时重新应用。

### 3.4.6 工作区隔离

- 高亮配置保存在当前工作区 `.vscode/novel-assistant.json`。
- 不同工作区互不影响。
- 全局设置只提供默认值。

### 3.4.7 验收标准

- 选中“张三”右键添加到“主角”，立即高亮。
- 重启 VSCode 后高亮保留。
- 修改颜色后立即生效。
- 不同工作区同名高亮组互不干扰。
- 高亮不影响字数统计。

---

## 3.5 工作区配置隔离

### 3.5.1 目标

不同小说工作区拥有独立配置。  
包括字数模式、高亮组、目标、番茄钟、敏感词、排除文件、格式化规则。

### 3.5.2 配置文件

MUST：

- 工作区配置：`.vscode/novel-assistant.json`
- 工作区统计：`.vscode/novel-assistant-stats.json`
- 全局默认：VSCode Settings 中 `novelAssistant.*`

优先级：

```text
工作区配置 > 全局配置 > 内置默认值
```

### 3.5.3 初始化

首次打开工作区时：

- 若不存在 `.vscode/novel-assistant.json`，扩展可提示“是否初始化小说助手配置”。
- 用户确认后生成默认配置。
- 不自动生成，避免污染工作区。

### 3.5.4 配置版本

配置文件包含 `version` 字段。  
升级扩展时，自动迁移旧配置。  
迁移前备份为 `.vscode/novel-assistant.backup.json`。

### 3.5.5 多根工作区

- 每个根文件夹独立配置。
- 状态栏显示当前文件所属根文件夹。
- 若当前文件不属于任何根文件夹，使用全局配置。

### 3.5.6 验收标准

- 工作区 A 设置“仅中文”，工作区 B 设置“起点模式”，互不影响。
- 高亮组、每日目标、番茄钟配置同理。

---

## 3.6 番茄钟与专注模式

### 3.6.1 目标

帮助用户进入专注写作状态。  
将专注时间与字数产出绑定。  
提供工作、短休、长休循环。

### 3.6.2 默认参数

- 工作：25 分钟。
- 短休：5 分钟。
- 长休：15 分钟。
- 每 4 个番茄后长休。
- 工作时长可配置范围：1~120 分钟。
- 短休范围：1~60 分钟。
- 长休范围：1~120 分钟。
- 循环数范围：1~12。

### 3.6.3 状态

- 空闲
- 工作中
- 短休
- 长休
- 暂停
- 已中断

### 3.6.4 计时规则

MUST：

- 仅当窗口活动时倒计时。
- 窗口失焦时自动暂停。
- 窗口重新聚焦后，不自动继续，需用户确认或按配置自动继续。默认自动继续。
- 空闲是否暂停可配置，默认不暂停。

### 3.6.5 开始与结束

开始方式：

- 命令面板：`小说助手：开始番茄钟`。
- 状态栏番茄图标点击。
- 侧边栏按钮。

结束：

- 倒计时结束，系统通知 + VSCode 信息消息。
- 显示本次报告：
  - 专注时长。
  - 新增字数。
  - 输入速度。
  - 是否达到目标。
- 自动进入休息或等待用户确认，可配置。

中断：

- 用户可手动停止。
- 停止时选择：
  - 放弃本次番茄。
  - 保存已专注时间。
- 默认保存已专注时间，但不计入完整番茄数。

### 3.6.6 与专注模式联动

开始番茄钟时，可自动：

- 隐藏侧边栏。
- 隐藏面板。
- 进入全屏。
- 开启打字机模式。
- 开启对话高亮。
- 关闭通知。

默认不自动，提供“番茄钟联动专注模式”开关。

### 3.6.7 统计

记录：

- 今日完成番茄数。
- 今日专注总时长。
- 今日专注总字数。
- 历史番茄记录。
- 每个番茄的详细报告。

### 3.6.8 验收标准

- 开始 25 分钟番茄，窗口失焦后倒计时暂停。
- 结束后显示本次字数与速度。
- 完成 4 个番茄后自动进入长休。
- 重启后今日番茄数保留。

---

## 3.7 每日目标、写作热力图与日历

### 3.7.1 每日目标

支持目标类型：

- 每日字数目标。
- 每日创作时间目标。
- 每日番茄数目标。
- 每日有效输入时间目标。

默认：

- 每日字数：2000 字。
- 每日创作时间：60 分钟。
- 每日番茄数：4 个。

状态栏显示：

```text
🎯 62% (1240/2000 字)
```

达成时：

- 状态栏变色。
- 可选通知。
- 可选声音。

### 3.7.2 写作热力图

- 展示过去 365 天每日写作字数。
- 颜色分级：0、1-500、501-1000、1001-2000、2001-4000、4000+。
- 点击某天查看详情：字数、创作时间、番茄数、速度。
- 数据来自统计文件。

### 3.7.3 写作日历

- 月视图展示每日字数。
- 支持切换月份。
- 显示目标达成标记。

### 3.7.4 验收标准

- 今日字数达到目标后，状态栏进度变化。
- 热力图颜色随历史数据变化。
- 数据持久化。

---

## 3.8 敏感词检测

### 3.8.1 目标

帮助网文作者规避平台审核风险。  
本地检测，不上传文本。  
支持自定义词库。

### 3.8.2 词库分级

默认三级：

- 严重：红色背景 + 波浪线。
- 警告：橙色下划线。
- 提示：黄色下划线。

词库来源：

- 内置基础词库，可关闭。
- 工作区自定义词库：`.vscode/novel-assistant-sensitive.txt`。
- 用户可导入 JSON/CSV。

### 3.8.3 检测规则

- 输入防抖 `500ms`。
- 扫描当前文档。
- 支持全词匹配、大小写、正则。
- 支持排除白名单。
- 问题面板显示所有命中。
- 点击问题跳转。

### 3.8.4 管理

命令：`小说助手：管理敏感词`  
QuickPick：

- 添加敏感词。
- 删除敏感词。
- 切换分级。
- 启用/禁用。
- 导入/导出词库。

### 3.8.5 验收标准

- 输入敏感词后，500ms 内出现高亮。
- 删除后高亮消失。
- 词库按工作区隔离。

---

## 3.9 章节结构面板

### 3.9.1 目标

在侧边栏管理小说章节。  
显示字数、状态、排序。  
快速跳转。

### 3.9.2 展示

树形或列表：

- 章节名。
- 字数。
- 状态：草稿、修改中、完成。
- 最后编辑时间。
- 是否包含敏感词。
- 是否未回收伏笔。

排序：

- 文件名自然排序。
- 修改时间。
- 字数。
- 手动排序。

### 3.9.3 操作

- 点击打开章节。
- 右键：重命名、删除、新建章节、设置状态。
- 新建章节：输入章节名，自动创建文件并应用格式化。
- 批量统计全书字数。

### 3.9.4 验收标准

- 新增章节后自动出现在面板。
- 字数随编辑更新。
- 状态保存到工作区配置或统计文件。

---

## 3.10 角色引用热力图

### 3.10.1 目标

统计角色在各章节中的出场频次。  
用颜色深浅展示角色活跃度。  
帮助作者平衡戏份。

### 3.10.2 数据来源

- 高亮组“主角”“配角”中的词。
- 或独立角色管理列表。
- 每个角色可设置别名。

### 3.10.3 展示

表格：

- 行：角色。
- 列：章节。
- 单元格：出现次数。
- 颜色：次数越多颜色越深。
- 点击单元格跳转到对应章节。

### 3.10.4 验收标准

- 角色名出现后统计更新。
- 别名合并统计。
- 热力图可导出。

---

## 3.11 故事时间线

### 3.11.1 目标

管理故事事件，理清剧情脉络。

### 3.11.2 事件字段

- 标题。
- 描述。
- 所属章节。
- 故事内时间。
- 事件类型：战斗、感情、转折、日常、伏笔、其他。
- 状态：计划、已写、已修改、废弃。
- 关联角色。
- 关联地点。

### 3.11.3 展示

侧边栏列表或看板。  
支持按章节、时间、类型筛选。  
点击跳转章节。

### 3.11.4 验收标准

- 增删改查正常。
- 数据按工作区保存。
- 重启后保留。

---

## 3.12 伏笔追踪

### 3.12.1 目标

追踪伏笔生命周期：埋设、推进、回收。  
避免挖坑不填。

### 3.12.2 伏笔字段

- 名称。
- 描述。
- 埋设章节。
- 推进章节列表。
- 回收章节。
- 状态：未回收、部分回收、已回收、废弃。
- 优先级。

### 3.12.3 展示

侧边栏列表。  
未回收伏笔在章节面板中标记。  
支持筛选。

### 3.12.4 验收标准

- 标记回收后状态更新。
- 未回收伏笔有提醒。
- 数据持久化。

---

## 3.13 写作冲刺

### 3.13.1 目标

短时间高强度写作。  
与番茄钟区别：冲刺不强制休息，更强调速度。

### 3.13.2 参数

- 时长：5、10、15、30、60 分钟。
- 可选目标字数。
- 倒计时。
- 结束报告：字数、速度、是否达标。

### 3.13.3 触发

命令：`小说助手：开始写作冲刺`。  
状态栏或侧边栏入口。

### 3.13.4 验收标准

- 倒计时结束显示报告。
- 窗口失焦暂停。
- 记录到统计。

---

## 3.14 对话标记增强

### 3.14.1 目标

区分叙述与对话，提升可读性。

### 3.14.2 规则

识别对话行：

- 以中文引号开头：`“`、`‘`、`「`、`『`。
- 以破折号开头：`——`。
- 以英文引号开头：`"`、`'`。

对话行可设置：

- 前景色。
- 背景色。
- 斜体。
- 加粗。

默认关闭。  
使用装饰器实现，不修改文本。

### 3.14.3 验收标准

- 开启后对话行变色。
- 关闭后恢复。
- 不影响高亮系统。

---

## 3.15 打字机模式

### 3.15.1 目标

光标保持在屏幕中央，减少视线移动。

### 3.15.2 行为

- 当前行始终位于编辑器垂直中央。
- 滚动由扩展控制。
- 可配置开关。
- 与番茄钟联动可选。

### 3.15.3 验收标准

- 开启后输入时光标位置稳定。
- 关闭后恢复默认滚动。

---

## 4. UI 与交互总览

### 4.1 状态栏

MUST 显示：

- 当前文档字数。
- 当前计数模式。
- 今日创作时间。
- 当前输入速度。
- 今日目标进度。
- 番茄钟状态，若运行中。

点击行为：

- 字数：打开统计详情。
- 时间：暂停/继续。
- 速度：打开速度详情。
- 目标：打开目标设置。
- 番茄：开始/暂停/停止。

### 4.2 侧边栏

视图容器：`小说助手`  
包含：

- 章节面板
- 高亮管理
- 角色热力图
- 时间线
- 伏笔追踪
- 写作统计
- 敏感词
- 番茄钟/冲刺

### 4.3 命令面板

所有命令使用中文，避免复杂英文。  
命令列表见第 12 节。

### 4.4 右键菜单

编辑器右键：

- `小说助手：添加到高亮组`
- `小说助手：格式化选中段落`
- `小说助手：统计选中文本`
- `小说助手：将选中文本设为角色`
- `小说助手：将选中文本设为关键道具`

### 4.5 QuickPick

所有配置优先使用 QuickPick。  
例如：

- 选择高亮组。
- 选择颜色。
- 选择计数模式。
- 选择番茄钟时长。
- 选择目标类型。

### 4.6 通知

重要事件通知：

- 番茄钟结束。
- 冲刺结束。
- 每日目标达成。
- 配置初始化完成。
- 敏感词词库导入完成。

---

## 5. 配置与数据结构

### 5.1 工作区配置示例

`.vscode/novel-assistant.json`

```json
{
  "version": 1,
  "fileExtensions": [".txt"],
  "excludeGlobs": [".vscode/**", "node_modules/**", ".git/**"],
  "countMode": "qidian",
  "countModes": {
    "qidian": { "type": "non_whitespace" },
    "fanqie": { "type": "fanqie" },
    "cjk": { "type": "cjk" },
    "custom": {
      "type": "custom_weight",
      "rules": [
        { "type": "cjk", "weight": 1 },
        { "type": "punctuation", "weight": 1 },
        { "type": "whitespace", "weight": 0 },
        { "type": "ascii_word", "weight": 1 },
        { "type": "digit_sequence", "weight": 1 }
      ]
    }
  },
  "autoFormatOnSave": false,
  "formatIndent": "    ",
  "skipTitleIndent": true,
  "titlePatterns": [
    "^第[一二三四五六七八九十百千万零两0-9]+[章回节卷部篇]",
    "^Chapter\\s+\\d+",
    "^序章",
    "^楔子",
    "^尾声",
    "^后记"
  ],
  "idleThresholdSeconds": 120,
  "pauseCreativeTimeWhenIdle": false,
  "pasteThresholdChars": 30,
  "pasteIntervalMs": 500,
  "highlights": [
    {
      "id": "protagonist",
      "name": "主角",
      "color": "#FFD700",
      "enabled": true,
      "priority": 1,
      "words": ["张三"],
      "matchCase": false,
      "wholeWord": false,
      "regex": false,
      "style": "background"
    },
    {
      "id": "supporting",
      "name": "配角",
      "color": "#87CEEB",
      "enabled": true,
      "priority": 2,
      "words": ["李四"],
      "matchCase": false,
      "wholeWord": false,
      "regex": false,
      "style": "background"
    }
  ],
  "sensitiveWords": {
    "enabled": true,
    "levels": {
      "severe": ["词1"],
      "warning": ["词2"],
      "info": ["词3"]
    }
  },
  "goals": {
    "dailyWords": 2000,
    "dailyMinutes": 60,
    "dailyPomodoros": 4,
    "dailyActiveMinutes": 45
  },
  "pomodoro": {
    "workMinutes": 25,
    "shortBreakMinutes": 5,
    "longBreakMinutes": 15,
    "cyclesBeforeLongBreak": 4,
    "autoContinue": true,
    "pauseWhenIdle": false
  },
  "sprint": {
    "defaultMinutes": 15,
    "defaultTargetWords": 500
  },
  "typewriterMode": false,
  "dialogueHighlight": false,
  "focusMode": {
    "hideSidebar": true,
    "hidePanel": true,
    "fullScreen": false
  }
}
```

### 5.2 统计数据结构

`.vscode/novel-assistant-stats.json`

```json
{
  "version": 1,
  "daily": {
    "2026-09-24": {
      "words": 1234,
      "activeSeconds": 3600,
      "inputSeconds": 2400,
      "pomodoros": 2,
      "sprints": 1,
      "speed": 30.85
    }
  },
  "documents": {
    "chapter1.txt": {
      "words": 3456,
      "activeSeconds": 7200,
      "lastEdited": "2026-09-24T12:00:00+08:00"
    }
  },
  "sessions": [
    {
      "start": "2026-09-24T10:00:00+08:00",
      "end": "2026-09-24T11:00:00+08:00",
      "words": 800,
      "activeSeconds": 3600,
      "inputSeconds": 2400
    }
  ],
  "pomodoroHistory": [],
  "sprintHistory": []
}
```

### 5.3 全局设置

VSCode Settings 中：

```json
{
  "novelAssistant.defaultCountMode": "qidian",
  "novelAssistant.defaultDailyWords": 2000,
  "novelAssistant.defaultPomodoroWorkMinutes": 25
}
```

---

## 6. 数据持久化与隐私

- 所有数据默认存储在本地。
- 不上传任何文本、统计、高亮、敏感词。
- 工作区配置可提交到 Git，统计文件建议加入 `.gitignore`。
- 提供“清除本工作区统计数据”命令。
- 提供“导出全部数据”命令，格式 JSON。
- 提供“导入数据”命令，需校验版本。

---

## 7. 边界条件与异常处理

### 7.1 文件相关

- 空文件：字数 0，格式化不操作。
- 超大文件：超过 10MB 时，提示性能限制，降低刷新频率。
- 非 UTF-8：由 VSCode 处理，扩展按字符串处理。
- 多根工作区：按根文件夹隔离。
- 文件被外部修改：重新读取并更新统计。

### 7.2 计时相关

- 窗口失焦：暂停所有计时。
- 多窗口同一工作区：只有焦点窗口计时。
- 系统休眠：恢复后不补计休眠时间。
- 跨天：按本地时区切分。
- 手动修改系统时间：以当前时间为准，不处理异常。

### 7.3 高亮相关

- 匹配词为空：忽略。
- 正则无效：提示错误，不应用。
- 匹配过多：限制 5000，提示用户缩小范围。
- 颜色无效：回退默认色。
- 组名重复：允许，但 ID 必须唯一。

### 7.4 番茄钟相关

- 倒计时中关闭 VSCode：保存已专注时间。
- 倒计时中切换工作区：暂停当前番茄，询问是否继续。
- 系统通知不可用：使用 VSCode 消息。

### 7.5 输入速度相关

- 粘贴大段：不计入速度。
- 输入法组合：提交后计数。
- 撤销：近似处理，不计入速度。
- 删除：不计入新增。

---

## 8. 非功能需求

### 8.1 性能

- 状态栏更新防抖 300ms。
- 高亮匹配防抖 200ms。
- 敏感词检测防抖 500ms。
- 大文件不阻塞主线程。
- 内存占用尽量低于 100MB。

### 8.2 兼容性

- VSCode 版本：1.80+。
- 操作系统：Windows、macOS、Linux。
- 中文界面优先，可扩展英文。

### 8.3 可访问性

- 颜色对比度符合基本可读性。
- 支持键盘操作 QuickPick。
- 状态栏信息不依赖颜色单独传达。

### 8.4 测试

- 计数引擎单元测试。
- 计时状态机单元测试。
- 格式化规则单元测试。
- 高亮匹配单元测试。
- 交互集成测试。

---

## 9. 技术架构建议

### 9.1 语言与框架

- TypeScript。
- VSCode Extension API。
- 可选 Webview 用于统计面板、热力图、时间线看板。

### 9.2 模块划分

```text
src/
  extension.ts
  config/
    configService.ts
    defaultConfig.ts
    migration.ts
  count/
    countEngine.ts
    modes/
      all.ts
      cjk.ts
      cjkPunct.ts
      nonWhitespace.ts
      fanqie.ts
      mixed.ts
      customRegex.ts
      customWeight.ts
  timer/
    timerService.ts
    focusWatcher.ts
    inputSpeedService.ts
  format/
    formatService.ts
    titleDetector.ts
  highlight/
    highlightService.ts
    decorationManager.ts
    highlightGroupService.ts
  pomodoro/
    pomodoroService.ts
    pomodoroState.ts
  sprint/
    sprintService.ts
  sensitive/
    sensitiveWordService.ts
  stats/
    statsService.ts
    heatmapService.ts
    goalService.ts
  chapters/
    chapterService.ts
    chapterTreeProvider.ts
  characters/
    characterService.ts
    characterHeatmapService.ts
  timeline/
    timelineService.ts
  foreshadow/
    foreshadowService.ts
  ui/
    statusBar.ts
    sidebarProvider.ts
    quickPicks.ts
    commands.ts
  utils/
    debounce.ts
    text.ts
    unicode.ts
```

### 9.3 关键事件

- `window.onDidChangeWindowState`
- `window.onDidChangeActiveTextEditor`
- `workspace.onDidChangeTextDocument`
- `workspace.onDidSaveTextDocument`
- `workspace.onDidOpenTextDocument`
- `workspace.onDidCloseTextDocument`
- `commands.registerCommand`

### 9.4 计数引擎设计

策略模式。  
每个模式实现：

```ts
interface CountMode {
  id: string;
  name: string;
  count(text: string): number;
}
```

支持异步 Worker 用于大文件。

### 9.5 计时服务设计

状态机：

```text
Idle -> Active -> Paused -> Active
```

事件：

- windowFocused
- windowBlurred
- textInserted
- textDeleted
- pasteDetected
- idleTimeout

### 9.6 高亮服务设计

- 缓存 DecorationType。
- 异步匹配。
- 防抖。
- 优先级排序。
- 只对可见编辑器应用。

---

## 10. 验收标准总表

### 10.1 字数统计

- 各模式算法正确。
- 状态栏实时更新。
- 工作区总字数正确。
- 切换模式后配置持久化。

### 10.2 创作时间

- 窗口活动才计时。
- 失焦暂停。
- 今日时间跨重启保留。
- 多窗口不重复计时。

### 10.3 输入速度

- 粘贴不计入。
- 空闲不计入。
- 速度数值合理。
- 显示单位正确。

### 10.4 格式化

- 每段四个半角空格。
- 标题跳过。
- 空行不变。
- 可撤销。

### 10.5 高亮

- 右键添加后立即高亮。
- 重启保留。
- 工作区隔离。
- 颜色可修改。
- 不影响文本。

### 10.6 番茄钟

- 25 分钟工作，5 分钟短休。
- 失焦暂停。
- 结束报告字数与速度。
- 4 个后长休。

### 10.7 其他

- 每日目标进度。
- 热力图。
- 敏感词。
- 章节面板。
- 角色热力图。
- 时间线。
- 伏笔。
- 冲刺。
- 对话高亮。
- 打字机模式。

---

## 11. 开发路线图

### 第一阶段：MVP

MUST：

- 字数统计多模式。
- 创作时间计时。
- 输入速度。
- 文本格式化。
- 高亮系统。
- 工作区配置隔离。
- 状态栏。
- 命令面板。
- 基础侧边栏。

### 第二阶段：写作管理

SHOULD：

- 番茄钟。
- 每日目标。
- 写作热力图。
- 敏感词检测。
- 章节结构面板。
- 写作冲刺。

### 第三阶段：剧情管理

MAY：

- 角色引用热力图。
- 故事时间线。
- 伏笔追踪。
- 对话标记增强。
- 打字机模式。
- 高级统计导出。

---

## 12. 附录：命令与默认值汇总

### 12.1 命令列表

- `小说助手：初始化配置`
- `小说助手：切换字数统计模式`
- `小说助手：查看字数统计详情`
- `小说助手：格式化当前文档`
- `小说助手：格式化选中段落`
- `小说助手：管理高亮组`
- `小说助手：添加选中文本到高亮组`
- `小说助手：开始番茄钟`
- `小说助手：暂停番茄钟`
- `小说助手：停止番茄钟`
- `小说助手：开始写作冲刺`
- `小说助手：打开写作统计`
- `小说助手：切换专注模式`
- `小说助手：切换打字机模式`
- `小说助手：管理敏感词`
- `小说助手：新建章节`
- `小说助手：重建章节索引`
- `小说助手：清除本工作区统计数据`
- `小说助手：导出数据`
- `小说助手：导入数据`

### 12.2 默认值

| 项目 | 默认值 |
|---|---|
| 文件扩展名 | `.txt` |
| 计数模式 | `qidian` |
| 自动保存格式化 | 关闭 |
| 缩进 | 四个半角空格 |
| 跳过标题缩进 | 开启 |
| 空闲阈值 | 120 秒 |
| 空闲暂停创作时间 | 关闭 |
| 粘贴阈值 | 30 字符 |
| 高亮防抖 | 200ms |
| 敏感词防抖 | 500ms |
| 番茄工作 | 25 分钟 |
| 短休 | 5 分钟 |
| 长休 | 15 分钟 |
| 长休前循环 | 4 个 |
| 每日字数目标 | 2000 字 |
| 每日时间目标 | 60 分钟 |
| 每日番茄目标 | 4 个 |

---

本文档结束。  
AI 在实现或回答相关问题时，应优先遵循本文档中的 MUST 条目；若需扩展功能，应保持工作区隔离、中文直观交互、本地优先、低干扰写作四项原则。