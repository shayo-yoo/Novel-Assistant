# 小说助手使用说明

## 1. 扩展简介

小说助手是一款面向中文小说创作者的 VS Code 扩展，覆盖写作全流程：

- 章节字数统计与 9 种计数模式
- 今日创作时间累计与输入速度观察
- 自动段落缩进格式化
- 工作区隔离的配置与高亮管理
- 番茄钟、写作冲刺与每日目标
- 敏感词本地检测
- 章节面板、角色热力图、故事时间线、伏笔追踪
- 对话高亮与打字机模式

它遵循“轻量、低干扰、工作区隔离、本地优先”的设计原则，所有数据默认只保存在本地。

---

## 2. 安装与启动

### 2.1 依赖安装

在扩展目录中执行：

```bash
npm install
```

### 2.2 编译扩展

```bash
npm run compile
```

### 2.3 运行方式

- 使用 VS Code 的 F5 启动 Extension Development Host
- 在新打开的开发宿主中打开你的小说目录
- 开始使用扩展命令、面板和状态栏功能

---

## 3. 常用入口

### 3.1 命令面板（Ctrl + Shift + P）

常用命令：

- 小说助手：初始化配置
- 小说助手：切换字数统计模式
- 小说助手：查看字数统计详情
- 小说助手：格式化当前文档 / 选中段落 / 整个工作区
- 小说助手：添加选中文本到高亮组
- 小说助手：管理高亮组
- 小说助手：开始番茄钟 / 开始写作冲刺
- 小说助手：管理敏感词
- 小说助手：新建章节 / 设置章节状态
- 小说助手：将选中文本设为角色 / 设为关键道具
- 小说助手：添加时间线事件 / 埋设伏笔
- 小说助手：打开写作统计 / 打开角色热力图
- 小说助手：导出数据 / 导入数据

### 3.2 右键菜单

在编辑器里选中文本后，右键菜单中可直接使用：

- 小说助手：添加选中文本到高亮组
- 小说助手：取消选中文本高亮
- 小说助手：将选中文本设为角色
- 小说助手：将选中文本设为关键道具
- 小说助手：格式化选中段落
- 小说助手：统计选中文本

### 3.3 状态栏

状态栏显示：

```text
📝 12,345 字 [起点] | ⏱ 1h23m | ⚡ 28 字/分 | 🎯 62% | 🍅 工作 24:30
```

各段含义：

- 字数：当前文档字数与模式（点击：详情/切换模式/配置自定义模式）
- 时间：今日创作时间（点击：暂停/继续计时）
- 速度：当前输入速度（点击：查看四种速度详情）
- 目标：今日字数目标进度（点击：设置每日目标）
- 番茄：番茄钟状态（点击：开始/暂停/停止）
- 冲刺：运行中显示（点击：暂停/停止）

### 3.4 侧边栏

活动栏“小说助手”包含 7 个视图：

| 视图 | 内容 |
|---|---|
| 章节面板 | 章节树、字数、状态、敏感词/伏笔标记 |
| 高亮管理 | 高亮组与词，右键管理 |
| 角色热力图 | 角色 × 章节出场热力图 |
| 故事时间线 | 事件分组与筛选 |
| 伏笔追踪 | 伏笔生命周期 |
| 写作统计 | 今日概览、目标进度、365 天热力图、月历、番茄/冲刺控制 |
| 敏感词 | 三级词库 |

---

## 4. 初始化工作区配置

1. 打开命令面板，运行 `小说助手：初始化配置`
2. 确认后生成 `.vscode/novel-assistant.json`

配置特点：

- 按工作区保存，不同小说目录互不影响
- 全局设置作为默认值，工作区配置优先
- 升级扩展时自动迁移旧配置并备份

---

## 5. 字数统计

### 5.1 计数模式

| 模式 | 说明 |
|---|---|
| `all` | 全字符：所有 Unicode 码点各计 1 |
| `cjk` | 仅中文：只统计汉字 |
| `cjk_punct` | 中文加标点：汉字 + 中文常用标点 |
| `non_whitespace` | 非空白字符 |
| `qidian` | 起点口径（同非空白，可自定义覆盖） |
| `fanqie` | 番茄口径：中文按字、英文按词、数字按串 |
| `mixed` | 中英混合 |
| `custom_regex` | 自定义正则（按匹配字符数或次数） |
| `custom_weight` | 自定义权重规则 |

### 5.2 切换模式

`小说助手：切换字数统计模式` → 选择模式，状态栏立即更新。

### 5.3 查看详情

`小说助手：查看字数统计详情` 显示各模式字数与当前选中文本字数。

### 5.4 建议

- 中文网文优先 `qidian`
- 重视真实汉字数量用 `cjk`
- 多语混排用 `mixed` 或 `fanqie`

---

## 6. 创作时间与输入速度

### 6.1 计时规则

- 窗口前台焦点 + 活动编辑器属于当前工作区时每秒累计
- 失焦立即暂停
- 空闲但未失焦默认不暂停（可配置空闲暂停，阈值 120 秒）
- 跨天按本地时区切分，按工作区/日期/文档累计

### 6.2 粘贴判定

单次插入 >30 字符且距上次输入 <500ms 视为粘贴：计入字数、不计入速度与有效输入时间。

### 6.3 速度

- 瞬时（60 秒）、短期（5 分钟，默认显示）、会话平均、今日平均
- 命令：`小说助手：查看输入速度详情`

---

## 7. 文本格式化

- `小说助手：格式化当前文档`：全文段首四空格缩进
- `小说助手：格式化选中段落`：仅选中范围
- `小说助手：格式化整个工作区`：批量处理全部小说文件
- 规则：空行不变、标题行/分隔线/引用/列表行默认跳过、前导空白规范化、保留 LF/CRLF、可一次撤销
- 可在配置中开启“保存时自动格式化”

---

## 8. 高亮系统

### 8.1 内置分组

主角（金）、配角（浅蓝）、关键道具（浅红）、地点（浅绿）、自定义（灰）。

### 8.2 添加高亮词

选中文本 → 右键 → `小说助手：添加选中文本到高亮组` → 选组，立即高亮。

### 8.3 管理高亮组

- 命令：`小说助手：管理高亮组`
- 侧边栏“高亮管理”：组节点右键可添加词、删词、重命名、改颜色、改样式、启停、删组

### 8.4 高级选项

- 正则匹配、区分大小写、全词匹配
- 优先级覆盖（数值小者优先）
- 最大匹配数 5000，超出后仅高亮可见区域

---

## 9. 番茄钟与专注模式

### 9.1 默认循环

工作 25 分钟 → 短休 5 分钟；每 4 个番茄后长休 15 分钟。

### 9.2 操作

- `小说助手：开始番茄钟` / 暂停 / 停止
- 状态栏番茄图标点击操作
- 写作统计侧边栏快捷按钮

### 9.3 行为

- 仅窗口活动时倒计时；失焦自动暂停，聚焦后按配置自动继续（默认自动）
- 结束报告：时长、新增字数、速度、是否达标
- 中断时选择“保存已专注时间”或“放弃”
- 参数可配置：`小说助手：设置番茄钟时长`

### 9.4 专注模式

`小说助手：切换专注模式`：按配置隐藏侧边栏/面板/全屏。`小说助手：配置专注模式动作` 选择联动项。

---

## 10. 每日目标与写作热力图

- 目标类型：字数（2000）、时间（60 分钟）、番茄数（4）、有效输入（45 分钟）
- 命令：`小说助手：设置每日目标`
- 达成时状态栏变色并通知（每天一次）
- 写作统计视图：365 天热力图（6 级色阶）+ 本月日历 + 今日概览

---

## 11. 敏感词检测

### 11.1 分级

- 严重：红底 + 波浪线
- 警告：橙色下划线
- 提示：黄色下划线

### 11.2 词库来源

- 工作区配置中的分级词库
- `.vscode/novel-assistant-sensitive.txt`（默认警告级）

### 11.3 操作

`小说助手：管理敏感词`：添加/删除/切换分级/启用禁用/导入（txt/json/csv）/导出。

检测结果同时显示在编辑器和问题面板，点击问题可跳转。

---

## 12. 章节面板

- 树形展示：名称、字数、状态（草稿/修改中/完成）、最后编辑时间
- ⚠️ 表示含敏感词；🪝 表示含未回收伏笔
- 右键章节：重命名、删除、设置状态
- 标题栏按钮：刷新、新建章节、按文件名/字数/修改时间排序

---

## 13. 角色热力图

- 选中人名 → 右键 → `小说助手：将选中文本设为角色`
- 选中道具名 → `小说助手：将选中文本设为关键道具`
- 侧边栏“角色热力图”：行=角色、列=章节、颜色随次数加深
- 点击单元格跳转章节；`小说助手：导出角色热力图 CSV`
- 角色管理：`小说助手：管理角色`（含别名合并统计）

---

## 14. 故事时间线

- 命令：`小说助手：添加时间线事件`
- 字段：标题、描述、章节、故事内时间、类型、状态、角色、地点
- 侧边栏按类型分组；`小说助手：筛选时间线`
- 右键事件：编辑、设置状态、删除；点击跳转章节

---

## 15. 伏笔追踪

- `小说助手：埋设伏笔` / `推进伏笔` / `回收伏笔`
- 生命周期：未回收 → 部分回收 → 已回收（或废弃）
- 未回收伏笔在章节面板中以 🪝 标记
- 优先级排序，右键可推进/回收/设置状态/删除

---

## 16. 写作冲刺

- `小说助手：开始写作冲刺`：选择 5/10/15/30/60 分钟与目标字数
- 失焦自动暂停，结束报告字数/速度/是否达标
- 状态栏与写作统计视图同步显示

---

## 17. 对话高亮与打字机模式

- `小说助手：切换对话高亮`：以 “ ‘ 「 『 ” ’ " —— 开头的对话行自动着色
- `小说助手：切换打字机模式`：当前行保持在编辑器中央

---

## 18. 数据管理

- `小说助手：导出全部数据`：JSON 格式（配置 + 统计 + 故事数据 + 词库）
- `小说助手：导入数据`：校验后恢复
- `小说助手：清除本工作区统计数据`：清空统计文件

建议将 `.vscode/novel-assistant-stats.json` 与 `-data.json` 加入 `.gitignore`。

---

## 19. 常见问题

### Q：我想统计不同口径的字数怎么办？

`小说助手：切换字数统计模式`，或配置自定义正则/权重模式。

### Q：我只想给某一段做格式化怎么办？

选中段落 → `小说助手：格式化选中段落`（或右键菜单）。

### Q：怎么把人物名高亮并计入角色热力图？

选中文本 → 右键 → 添加到高亮组；或右键 → 设为角色。

### Q：配置会不会混用到其他小说项目？

不会。每个工作区有独立的 `.vscode/novel-assistant.json`。

### Q：数据会上传云端吗？

不会。扩展使用本地文件保存数据，默认不联网。

### Q：番茄钟失焦了怎么办？

自动暂停；重新聚焦后按配置自动继续（默认自动）。也可以手动暂停/继续。

### Q：为什么粘贴大量文字速度没有变化？

粘贴（单次 >30 字符且间隔 <500ms）不计入输入速度，这是有意为之。

---

## 20. 总结

小说助手的核心价值：

- 让写作统计更直观
- 让小说格式更统一
- 让人物、伏笔、时间线更容易追踪
- 保持本地、私密、低干扰的创作体验
